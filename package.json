{
  "name": "claude-peers",
  "version": "0.1.1",
  "description": "Peer discovery and messaging for Claude Code instances",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "broker": "bun broker.ts",
    "server": "bun server.ts",
    "bench": "bun bench/delivery-latency.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "@modelcontextprotocol/sdk": "^1.27.1"
  }
}
